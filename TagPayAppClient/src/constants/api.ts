// src/constants/api.ts 주소 주소 주소 주소 여기라고 주소 주소 주소 주소 주소 주소 아 제발 여기 주소 주소 주소 주소 아 되어라 얍

import axios from 'axios';

const instance = axios.create({
  baseURL: ' https://d556-220-69-218-252.ngrok-free.app',
  timeout: 5000, // optional
});

export default instance;
